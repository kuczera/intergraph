import { SortFilterLabelByPipe } from './sort-filter-label-by.pipe';

describe('SortFilterLabelByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFilterLabelByPipe();
    expect(pipe).toBeTruthy();
  });
});
